/*
#include <stdio.h>

void swap(int* x, int* y);

int main() {

	int x, y;
	x = 10;
	y = 20;

	swap(&x, &y);

	printf("%d, %d\n", x, y);
	return 0;
}

void swap(int *x, int* y)
{
	int tmp;

	tmp = *y;
	*y = *x;
	*x = tmp;

}
*/
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "menuArray.h"

int main()
{

	score();

}